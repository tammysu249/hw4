<h1><%= @place.name %></h1>


<% if @current_user %>
  <h3>Posts</h3>

  <ul>
    <% for post in @place.posts.where({ user_id: session[:user_id] }) %>
       <h3><%= post.title %></h3>
       <h4><%= post.posted_on %></h4>
       <p><%= post.description %></p>
    <% end %>
  </ul>


<% end %>
